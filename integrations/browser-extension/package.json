{
  "name": "pressprotocol-extension",
  "version": "2.0.0",
  "description": "Browser extension for censorship-resistant content access and sovereign web clipping",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/background.ts src/popup.ts src/clipper.ts --bundle --outdir=dist --target=chrome100 --format=esm && pnpm run copy",
    "copy": "cp manifest.json dist/ && cp popup.html dist/ && cp popup.css dist/ && cp -r icons dist/",
    "watch": "esbuild src/background.ts src/popup.ts src/clipper.ts --bundle --outdir=dist --target=chrome100 --format=esm --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@noble/ed25519": "^2.3.0"
  }
}
