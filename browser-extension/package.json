{
  "name": "anonpress-extension",
  "version": "1.0.0",
  "description": "Browser extension for censorship-resistant content access",
  "scripts": {
    "build": "tsc && npm run copy",
    "copy": "cp manifest.json dist/ && cp popup.html dist/ && cp popup.css dist/ && mkdir -p dist/icons",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.6.3"
  }
}
